@model AdmissionSystem.View_Model.percentage_count_for_each_country_view_model

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Pages/Shared/_Layout_For_Admin_Dashboard.cshtml";
}

<h1 class="text-center">Edit percentage count </h1>

@*<h4>percentage_count_for_each_country_view_model</h4>*@
<hr />
<style>
    .content {
        max-width: 500px;
        margin: auto;
    }
</style>
<div class="row">
    <div class="col-md-4 content">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*English*@

            @if (ViewBag.Message != null)
            {
                <div class="alert alert-dark border-dark">
                    @ViewBag.Message

                </div>
            }
            @if (ViewBag.UpdateSuccess != null)
            {

                <div class="alert alert-success border-success">

                    @ViewBag.UpdateSuccess
                </div>
            }
            @if (ViewBag.sameEdit != null)
            {

                <div class="alert alert-warning border-warning">
                    @ViewBag.sameEdit
                </div>
            }
            @if (ViewBag.updatefails != null)
            {

                <div class="alert alert-danger border-dark">

                    @ViewBag.updatefails
                </div>
            }


            @*Arabic*@


            @*@if (ViewBag.MessageArabic != null)
        {
            <div class="alert alert-dark border-dark">
                @ViewBag.MessageArabic

            </div>
        }
        @if (ViewBag.UpdateSuccessArabic != null)
        {

            <div class="alert alert-success border-success">

                @ViewBag.UpdateSuccessArabic
            </div>
        }
        @if (ViewBag.sameEditArabic != null)
        {

            <div class="alert alert-warning border-warning">
                @ViewBag.sameEditArabic
            </div>
        }
        @if (ViewBag.updatefailsArabic != null)
        {

            <div class="alert alert-danger border-dark">

                @ViewBag.updatefailsArabic
            </div>
        }*@




            <input type="hidden" asp-for="id" />
            <div class="form-group">
                <label asp-for="statues_of_adm_eli_ID" class="control-label"></label>
                <select asp-for="statues_of_adm_eli_ID" class="form-control" asp-items="@(new SelectList(Model.FK_statues_of_admission_eligibilty,"id","Type_of_admission_eligibilty"))">
                </select>

                <span asp-validation-for="statues_of_adm_eli_ID" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="countryId" />
            <div class="form-group">
                <label asp-for="countryId" class="control-label"></label>
                <select name="countryseltectd" asp-for="countryId" class="form-control" disabled asp-items="@(new SelectList(Model.FK_country,"id","country_name") )">
                </select>
                <span asp-validation-for="countryId" class="text-danger"></span>
                @*<input type="hidden" asp-for="countryId" value=@Model.countryId />*@
            </div>
            <div class="form-group">
                <label asp-for="Rate" class="control-label"></label>
                <input asp-for="Rate" class="form-control " />
                <span asp-validation-for="Rate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-outline-primary" />
            </div>
            <div>
                <a asp-action="Index" class="btn btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
