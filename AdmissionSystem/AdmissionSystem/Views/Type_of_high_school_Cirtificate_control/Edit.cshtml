@model AdmissionSystem.Model.Type_of_high_school_Cirtificate

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Pages/Shared/_Layout_For_Admin_Dashboard.cshtml";
}

<h1 class="text-center">Edit Type of high school Cirtificate</h1>

@*<h4>Type_of_high_school_Cirtificate</h4>*@
<hr />
<style>
    .content {
        max-width: 500px;
        margin: auto;
    }
</style>
<div class="row">
    <div class="col-md-4 content">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*English*@

            @*@if (ViewBag.Message != null)
        {
            <div class="alert alert-dark border-dark">
                @ViewBag.Message

            </div>
        }*@
            @if (ViewBag.UpdateSuccess != null)
            {

                <div class="alert alert-success border-success">

                    @ViewBag.UpdateSuccess
                </div>
            }
            @if (ViewBag.sameEdit != null)
            {

                <div class="alert alert-warning border-warning">
                    @ViewBag.sameEdit
                </div>
            }
            @if (ViewBag.updatefails != null)
            {

                <div class="alert alert-danger border-dark">

                    @ViewBag.updatefails
                </div>
            }


            @*Arabic*@


            @*@if (ViewBag.MessageArabic != null)
        {
            <div class="alert alert-dark border-dark">
                @ViewBag.MessageArabic

            </div>
        }
        @if (ViewBag.UpdateSuccessArabic != null)
        {

            <div class="alert alert-success border-success">

                @ViewBag.UpdateSuccessArabic
            </div>
        }
        @if (ViewBag.sameEditArabic != null)
        {

            <div class="alert alert-warning border-warning">
                @ViewBag.sameEditArabic
            </div>
        }
        @if (ViewBag.updatefailsArabic != null)
        {

            <div class="alert alert-danger border-dark">

                @ViewBag.updatefailsArabic
            </div>
        }*@
          
            <div class="form-group">
                <label asp-for="type" class="control-label"></label>
                <input asp-for="type" class="form-control" />
                <span asp-validation-for="type" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-outline-primary" />
            </div>
            <div>
                <a asp-action="Index" class="btn btn-secondary btn-circle  ">
                    <i class="fas fa-arrow-circle-left"></i>
                </a>
            </div>
        </form>
    </div>
</div>

@*<div>
    <a asp-action="Index">Back to List</a>
</div>*@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
