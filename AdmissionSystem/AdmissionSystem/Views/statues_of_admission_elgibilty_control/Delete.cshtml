@model AdmissionSystem.Model.Statues_of_admission_eligibilty
@using System.Globalization
@using Microsoft.AspNetCore.Mvc.Localization

@inject IViewLocalizer localizer

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Pages/Shared/_Layout_For_Admin_Dashboard.cshtml";
}

<h1 class="text-center">@localizer["Delete an admission"]</h1>

<h3>@localizer["Are you sure you want to delete this?"]</h3>
<div>
  @*  <h4>Statues_of_admission_eligibilty</h4>*@
    <hr />
    
    
    <form asp-action="Delete">



        @*Englis*@
        @if (ViewBag.linkedDepartment != null)
        {
            <div class="alert alert-danger border-danger" role="alert">
                @ViewBag.linkedDepartment
            </div>
        }
        @if (ViewBag.freeDepartment != null)
        {
            <div class="alert alert-success border-success " role="alert">
                @ViewBag.freeDepartment
            </div>
        }
        @if (ViewBag.WasDeleted != null)
        {
            <div class="alert alert-warning border-warning " role="alert">
                @ViewBag.WasDeleted
            </div>
        }



        @*Arabic*@

        @*@if (ViewBag.linkedDepartmentArabic != null)
        {
            <div class="alert alert-danger border-danger" role="alert">
                @ViewBag.linkedDepartmentArabic
            </div>
        }
        @if (ViewBag.freeDepartmentArabic != null)
        {
            <div class="alert alert-success border-success " role="alert">
                @ViewBag.freeDepartmentArabic
            </div>
        }
        @if (ViewBag.WasDeletedArabic != null)
        {
            <div class="alert alert-warning border-warning " role="alert">
                @ViewBag.WasDeletedArabic
            </div>
        }*@



    <dl class="row">
        <dt class="col-sm-3">
         @localizer["Type of admission eligibilty"] 
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Type_of_admission_eligibilty)
        </dd>
        <dt class="col-sm-3">
         @localizer["Begaining date of the process"]  
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Begaining_date_of_the_process)
        </dd>
        <dt class="col-sm-3">
         @localizer["Semseter no"]  
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.semester_no)
        </dd>
        <dt class="col-sm-3">
        @localizer["semester Date"]   
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.semester_Date)
        </dd>
        <dt hidden class="col-sm-3">
         @Html.DisplayNameFor(model => model.Number_Student)
        </dt>
        <dd hidden class="col-sm-9">
            @Html.DisplayFor(model => model.Number_Student)
        </dd>
    </dl>




        <input type="hidden" asp-for="id" />
        <input type="submit" value="@localizer["Delete"]" class="btn btn-outline-danger" /> 
        <a asp-action="Index" class="btn btn-secondary">@localizer["Back to List"]</a>
    </form>
</div>
